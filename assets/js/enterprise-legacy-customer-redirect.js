(()=>{'use strict';const q=new URLSearchParams(location.search),kind=document.body.dataset.routeKind||'';async function call(body){const sb=window.screenings4uSupabase;const {data,error}=await sb.functions.invoke('enterprise-control-plane',{body:{action:'legacy_route_resolve',...body}});if(error)throw error;if(data?.error)throw new Error(data.error);return data}async function run(){try{let source_table='',source_id='';if(kind==='workforce_employer'){source_table='workforce.employers';source_id=q.get('id')||''}else{const type=q.get('type')||q.get('customer_type')||'employer';source_table='dot.'+(type==='owner_operator'?'owner_operators':type+'s');source_id=q.get('id')||q.get('customer_id')||''}if(!source_id)throw new Error('Customer ID is missing.');const r=await call({source_table,source_id,entity_type:'account'});if(!r.resolved)throw new Error('This legacy customer has not been linked to Enterprise yet. Run customer synchronization first.');const target=kind==='dot_portal_user'?'admin-customer-setup.html?account='+encodeURIComponent(r.account_id)+'&tab=manage':r.canonical_url;location.replace(target)}catch(e){document.getElementById('routeMessage').textContent=e.message||'Unable to resolve customer.'}}run()})();
