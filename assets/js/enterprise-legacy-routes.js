(()=>{'use strict';const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const rows=window.S4U_LEGACY_ROUTES||[];function label(s){return s==='canonical'?'Canonical':s==='redirect'?'Redirected':'Retained'}function draw(){const q=$('#routeSearch').value.trim().toLowerCase(),st=$('#routeStatus').value;const x=rows.filter(r=>(!st||r.status===st)&&(!q||JSON.stringify(r).toLowerCase().includes(q)));$('#routeCount').textContent=x.length+' routes';$('#routeRows').innerHTML=x.map(r=>`<tr><td class="ecp-name">${esc(r.file)}</td><td><span class="ecp-badge ${r.status==='canonical'?'active':r.status==='redirect'?'testing':'pending'}">${label(r.status)}</span></td><td>${esc(r.title||'Administrator page')}</td><td>${r.target?`<a href="${esc(r.target)}">${esc(r.target)}</a>`:'—'}</td><td>${r.status==='redirect'?`<a class="ecp-btn" href="${esc(r.target)}">Open canonical</a>`:`<a class="ecp-btn" href="${esc(r.file)}">Open</a>`}</td></tr>`).join('')||'<tr><td colspan="5">No matching routes.</td></tr>'}function init(){const c={canonical:rows.filter(r=>r.status==='canonical').length,redirect:rows.filter(r=>r.status==='redirect').length,retained:rows.filter(r=>r.status==='retained').length};$('#metrics').innerHTML=[['Canonical pages',c.canonical],['Legacy redirects',c.redirect],['Retained workflows',c.retained],['Admin routes',rows.length]].map(([a,b])=>`<div class="ecp-metric"><span>${a}</span><strong>${b}</strong></div>`).join('');$('#routeSearch').oninput=draw;$('#routeStatus').onchange=draw;draw()}document.addEventListener('DOMContentLoaded',init);})();