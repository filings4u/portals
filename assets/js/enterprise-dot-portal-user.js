(()=>{'use strict';
const END='https://wyezpseboxbmkedvbmyx.supabase.co/functions/v1/dot-enterprise-management',KEY='sb_publishable__BLewZS6h2V4yUczky-BTQ_EemiOdDL';const root=document.getElementById('dotPortalUser'),q=new URLSearchParams(location.search),id=q.get('id')||'',mode=q.get('mode')||'',customerType=q.get('customer_type')||'',customerId=q.get('customer_id')||'';const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function client(){for(let i=0;i<50;i++){const c=window.screenings4uSupabase||window.supabaseClient;if(c?.auth?.getSession)return c;await new Promise(r=>setTimeout(r,50))}throw new Error('Supabase client unavailable.');}async function call(action,extra={}){const c=await client(),{data:{session}}=await c.auth.getSession();if(!session)throw new Error('Your staff session has expired.');const r=await fetch(END,{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${session.access_token}`,'apikey':KEY},body:JSON.stringify({action,...extra})});const d=await r.json().catch(()=>({}));if(!r.ok||d.error)throw new Error(d.error||`Request failed (${r.status}).`);return d;}
function customerMap(d){return new Map([...(d.employers||[]).map(x=>[x.organization_id,{type:'employer',id:x.id,label:x.legal_name}]),...(d.ctpas||[]).map(x=>[x.organization_id,{type:'ctpa',id:x.id,label:x.organizations?.legal_name||x.organization_id}]),...(d.owner_operators||[]).map(x=>[x.organization_id,{type:'owner_operator',id:x.id,label:x.legal_name}])])}
function setStatus(text,kind=''){const e=document.getElementById('saveStatus');if(e){e.textContent=text;e.className='ep-record-status '+kind}}
async function load(){const d=await call('portal_access');const map=customerMap(d);if(mode==='new'){const target=(customerType&&customerId)?[...map.values()].find(x=>x.type===customerType&&x.id===customerId):null;const accounts=[...map.values()];root.innerHTML=`<div class="ep-record-hero"><div><span class="ep-eyebrow">DOT PORTAL ACCESS</span><h1>Invite Portal User</h1><p>Create or link a customer-facing DOT portal user to an authorized account.</p></div><div><a class="ep-business-btn" href="${target?`admin-dot-customer.html?type=${target.type}&id=${target.id}`:'admin-dot-portal-access.html'}">← Back</a></div></div><section class="ep-record-section"><form id="inviteForm" class="ep-record-form"><div class="ep-record-field full"><label>Customer</label><select name="account" required>${accounts.map(x=>`<option value="${x.type}:${x.id}" ${target&&x.type===target.type&&x.id===target.id?'selected':''}>${esc(x.type.replaceAll('_',' '))} · ${esc(x.label)}</option>`).join('')}</select></div><div class="ep-record-field"><label>First name</label><input name="first_name"></div><div class="ep-record-field"><label>Last name</label><input name="last_name"></div><div class="ep-record-field"><label>Email</label><input name="email" type="email" required></div><div class="ep-record-field"><label>Phone</label><input name="phone"></div><div class="ep-record-field full"><label>Role</label><select name="role_code" required>${['employer_admin','der','supervisor','hr_admin','ctpa_admin','ctpa_staff','owner_operator_admin','owner_operator_staff'].map(v=>`<option value="${v}">${v.replaceAll('_',' ')}</option>`).join('')}</select></div><div class="ep-record-savebar"><button class="ep-business-btn primary" type="submit">Send Invitation</button><span id="saveStatus" class="ep-record-status"></span></div></form></section>`;document.getElementById('inviteForm').addEventListener('submit',async e=>{e.preventDefault();setStatus('Sending…');try{const v=Object.fromEntries(new FormData(e.currentTarget).entries()),[account_type,account_id]=v.account.split(':');await call('invite_account_member',{account_type,account_id,member:v});setStatus('Invitation sent or account linked.','ok');setTimeout(()=>location.href=`admin-dot-customer.html?type=${account_type}&id=${account_id}#users`,650)}catch(err){setStatus(err.message,'error')}});return}
const m=(d.memberships||[]).find(x=>x.id===id);if(!m)throw new Error('Portal membership was not found.');const c=map.get(m.organization_id);root.innerHTML=`<div class="ep-record-hero"><div><span class="ep-eyebrow">DOT PORTAL ACCESS</span><h1>${esc(m.profiles?.display_name||[m.profiles?.first_name,m.profiles?.last_name].filter(Boolean).join(' ')||'Portal User')}</h1><p>${esc(c?.label||'Customer')} · ${esc(m.roles?.name||m.roles?.code||'Portal user')}</p></div><div class="ep-record-actions"><a class="ep-business-btn" href="${c?`admin-dot-customer.html?type=${c.type}&id=${c.id}#users`:'admin-dot-portal-access.html'}">← Customer</a></div></div><div class="ep-record-grid"><section class="ep-record-section"><div class="ep-record-section-head"><div><h2>Portal Membership</h2><p>Manage customer access status from a dedicated user record.</p></div></div><form id="membershipForm" class="ep-record-form"><div class="ep-record-field"><label>Name</label><input value="${esc(m.profiles?.display_name||[m.profiles?.first_name,m.profiles?.last_name].filter(Boolean).join(' '))}" disabled></div><div class="ep-record-field"><label>Role</label><input value="${esc(m.roles?.name||m.roles?.code||'—')}" disabled></div><div class="ep-record-field"><label>Portal</label><input value="${esc(m.roles?.portal_code||'—')}" disabled></div><div class="ep-record-field"><label>Status</label><select name="status">${['active','suspended','revoked'].map(v=>`<option value="${v}" ${m.status===v?'selected':''}>${v}</option>`).join('')}</select></div><div class="ep-record-savebar"><button class="ep-business-btn primary" type="submit">Save Membership</button><span id="saveStatus" class="ep-record-status"></span></div></form></section><aside class="ep-record-section"><h2>Customer</h2><dl class="ep-record-kv"><dt>Organization</dt><dd>${esc(c?.label||m.organization_id)}</dd><dt>User ID</dt><dd>${esc(m.user_id)}</dd><dt>Membership ID</dt><dd>${esc(m.id)}</dd><dt>Invited</dt><dd>${esc(m.invited_at||'—')}</dd><dt>Accepted</dt><dd>${esc(m.accepted_at||'—')}</dd></dl></aside></div>`;document.getElementById('membershipForm').addEventListener('submit',async e=>{e.preventDefault();setStatus('Saving…');try{const v=Object.fromEntries(new FormData(e.currentTarget).entries());await call('set_membership_status',{membership_id:id,status:v.status});setStatus('Membership updated.','ok')}catch(err){setStatus(err.message,'error')}})}
load().catch(e=>root.innerHTML=`<section class="ep-record-section"><h2>Portal user could not be loaded</h2><p>${esc(e.message)}</p><a class="ep-business-btn" href="admin-dot-portal-access.html">Back to Portal Access</a></section>`);
})();
