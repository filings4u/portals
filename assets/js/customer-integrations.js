(()=>{'use strict';
const $=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
function business(){const q=new URLSearchParams(location.search).get('business');if(q==='dot'||q==='workforce')return q;const h=location.hostname.toLowerCase();if(h.startsWith('dot-')||h.includes('dot-workforce'))return'dot';return'workforce'}
function msg(t='',e=false){const h=$('pageMessage');if(!h)return;h.className=t?'ci-message'+(e?' error':''):'';h.textContent=t}
async function api(body){const sb=window.screenings4uSupabase;const {data,error}=await sb.functions.invoke('customer-integration-portal',{body:{business:business(),...body}});if(error){let m=error.message;try{m=(await error.context?.clone?.().json())?.error||m}catch{}throw new Error(m)}if(data?.error)throw new Error(data.error);return data}
async function load(){try{msg('Loading integrations…');const d=await api({action:'workspace'}),rows=d.integrations||[];$('integrationList').innerHTML=rows.map(x=>{const c=x.integration,e=x.enablement;const state=e.status==='connected'?'<span class="ci-pill good">Connected</span>':e.status==='submitted'||e.status==='in_review'?'<span class="ci-pill warn">Configuration submitted</span>':e.status==='disabled'?'<span class="ci-pill off">Disabled</span>':'<span class="ci-pill">Ready to configure</span>';return `<article class="ci-item"><h3>${esc(c.name)}</h3><p>${esc(c.description||'')}</p><div class="ci-meta"><span class="ci-pill">${esc(c.category||'Integration')}</span><span class="ci-pill">${esc((c.auth_method||'manual').replaceAll('_',' '))}</span>${state}</div><a class="ci-btn primary" href="employer-integration.html?business=${encodeURIComponent(business())}&id=${encodeURIComponent(e.id)}">Manage Integration</a></article>`}).join('');$('emptyState').hidden=!!rows.length;msg('')}catch(e){msg(e.message||'Unable to load integrations.',true)}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',load,{once:true});else load();
})();
